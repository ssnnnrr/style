<Window x:Class="MyWPFApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Главное окно" Height="350" Width="525">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <TextBlock Grid.Row="0" Text="эщкере" Style="{StaticResource MyTextBlockStyle}" HorizontalAlignment="Center"/>
        <Button Grid.Row="1" Content="Кнопка 1" Style="{StaticResource MyButtonStyle}" Click="Button_Click" Margin="10,10,10,10"/>
        <Button Grid.Row="2" Content="Кнопка 2" Style="{StaticResource MyButtonStyle}" Click="Button_Click"/>
    </Grid>
</Window>